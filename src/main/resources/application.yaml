spring:
  application:
    name: blog
  datasource:
    url: jdbc:postgresql://localhost:5433/blog
    username: postgres
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

debug: true